import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarPerfil6Page } from './editar-perfil6.page';

describe('EditarPerfil6Page', () => {
  let component: EditarPerfil6Page;
  let fixture: ComponentFixture<EditarPerfil6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditarPerfil6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
