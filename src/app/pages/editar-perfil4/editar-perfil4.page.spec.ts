import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarPerfil4Page } from './editar-perfil4.page';

describe('EditarPerfil4Page', () => {
  let component: EditarPerfil4Page;
  let fixture: ComponentFixture<EditarPerfil4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditarPerfil4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
