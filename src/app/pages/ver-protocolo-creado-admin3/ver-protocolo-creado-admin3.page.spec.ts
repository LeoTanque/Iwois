import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VerProtocoloCreadoAdmin3Page } from './ver-protocolo-creado-admin3.page';

describe('VerProtocoloCreadoAdmin3Page', () => {
  let component: VerProtocoloCreadoAdmin3Page;
  let fixture: ComponentFixture<VerProtocoloCreadoAdmin3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(VerProtocoloCreadoAdmin3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
