import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarPerfil7Page } from './editar-perfil7.page';

describe('EditarPerfil7Page', () => {
  let component: EditarPerfil7Page;
  let fixture: ComponentFixture<EditarPerfil7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditarPerfil7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
