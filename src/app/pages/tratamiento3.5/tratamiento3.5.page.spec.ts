import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tratamiento35Page } from './tratamiento3.5.page';

describe('Tratamiento35Page', () => {
  let component: Tratamiento35Page;
  let fixture: ComponentFixture<Tratamiento35Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(Tratamiento35Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
