import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VerProtocoloCreadoAdmin2Page } from './ver-protocolo-creado-admin2.page';

describe('VerProtocoloCreadoAdmin2Page', () => {
  let component: VerProtocoloCreadoAdmin2Page;
  let fixture: ComponentFixture<VerProtocoloCreadoAdmin2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(VerProtocoloCreadoAdmin2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
